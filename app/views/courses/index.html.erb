<p id="notice"><%= notice %></p>

<h1>Course Page</h1>

<table>
  <thead>
    <tr>
      <th>Course</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.course_id %></td>
        <td><%= course.description %></td>
        <td><%= link_to 'Show', "/enrolls" %></td>
        <td><%= link_to 'Edit', edit_course_path(course) %></td>
        <td><%= link_to 'Delete', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p id="link"><%= link_to 'New Course', new_course_path %></p>

<!-- <p id="link", font-size:50><%= link_to "Show Enrolls", "/enrolls" %></p> -->
