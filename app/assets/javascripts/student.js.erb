// var st_name = <%= raw @name.to_json%>;
// var total = 0;
// for (var i = 0; i < st_name.length; i++) {
//   total += st_name[i];
// }

// var st_name = <%= raw @name.to_json%>;
// // var total = 0;
// for (var i = 0; i < st_name.length; i++) {
//   console.log(st_name[i]);
// }
// // console.log(total);
